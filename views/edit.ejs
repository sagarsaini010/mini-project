<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-10">

        <h5>Edit your post.</h5>

        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="Update your post"
                class="block p-3 outline-none w-1/3 bg-transparent border-2 border-zinc-700 rounded-md resize-none mt-2"
                name="content"><%= post.content %></textarea>

            <input type="submit" class="bg-yellow-500 mt-3 w-50 text-black text-sm rounded-md p-2" value="Update post">
        </form>
    </div>
</body>

</html>