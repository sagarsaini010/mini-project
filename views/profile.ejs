<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>

        </div>
        <h3 class="text-3xl"> <span class="font-light"> Hello,</span>
            <%= user.name %>
        </h3>
        <h5>You can create a new post.</h5>

        <form action="post" method="post">
            <textarea placeholder="What's in your mind?"
                class="block p-3 outline-none w-1/3 bg-transparent border-2 border-zinc-700 rounded-md resize-none mt-2"
                name="content"></textarea>

            <input type="submit" class="bg-blue-500 mt-3 w-50 text-sm rounded-md p-2" value="Create new post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400 text-2xl">Your posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach( (post)=> { %>

                    <div class="post w-1/3 p-4 mb-4 rounded-md border-1 border-zinc-600 bg-zinc-700">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %>
                        </h4>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>

                        <p class="mt-3"><%= post.likes.length %> likes</p>
                        <div class="btns flex mt-5 gap-10">
                            <a class="text-blue-500" href="/like/<%= post._id %>"> 
                               <%= post.likes.indexOf(user._id) === -1? 'Like' : 'Unlike' %></a>
                            <a class="text-zinc-500" href="/edit/<%= post._id %>">edit</a>
                        </div>
                    </div>

                    <% }) %>
            </div>
        </div>
    </div>
</body>

</html>